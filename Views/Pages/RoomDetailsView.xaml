<UserControl x:Class="SmartHouseApp.Views.Pages.RoomDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <StackPanel>
            <!-- Header -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="⬅ Back" Command="{Binding BackCommand}" Style="{StaticResource SecondaryButton}"/>
                <TextBlock Grid.Column="1" Text="{Binding Name}" Style="{StaticResource HeadingL}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>

            <!-- Device Controls -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Add Device: " VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Content="+ Light" Command="{Binding AddDeviceCommand}" CommandParameter="Light" Margin="5,0"/>
                    <Button Content="+ Thermostat" Command="{Binding AddDeviceCommand}" CommandParameter="Thermostat" Margin="5,0"/>
                    <Button Content="+ Door Lock" Command="{Binding AddDeviceCommand}" CommandParameter="DoorLock" Margin="5,0"/>
                </StackPanel>
            </Border>

            <!-- Device List -->
            <TextBlock Text="Devices in Room" Style="{StaticResource HeadingM}" Margin="0,0,0,10"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="350">
                <ItemsControl ItemsSource="{Binding Devices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Height="70" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="50"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Status}" Foreground="{Binding StatusColor}" FontSize="12"/>
                                    </StackPanel>
                                    
                                    <Button Grid.Column="2" Content="Toggle" Command="{Binding ToggleCommand}" Margin="5,0" Padding="10,4"/>
                                    <Button Grid.Column="3" Content="🗑 Delete" Command="{Binding DataContext.DeleteDeviceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="#EF4444" Margin="5,0" Padding="10,4"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>
