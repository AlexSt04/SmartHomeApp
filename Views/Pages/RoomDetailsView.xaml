<UserControl x:Class="SmartHouseApp.Views.Pages.RoomDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <StackPanel>
            <!-- Header -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="⬅ Back" Command="{Binding BackCommand}" Style="{StaticResource SecondaryButton}"/>
                <TextBlock Grid.Column="1" Text="{Binding Name}" Style="{StaticResource HeadingL}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>

            <!-- Device Controls -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Add Device: " VerticalAlignment="Center" Margin="0,0,10,0" Foreground="White"/>
                    
                    <TextBox Text="{Binding NewDeviceName, UpdateSourceTrigger=PropertyChanged}" Width="180" Margin="0,0,10,0" Tag="Device Name..."/>
                    
                    <ComboBox ItemsSource="{Binding DeviceTypes}" SelectedItem="{Binding SelectedDeviceType}" Width="120" Height="38" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                    
                    <Button Content="+ Add Device" Command="{Binding AddDeviceCommand}" Width="120"/>
                </StackPanel>
            </Border>

            <!-- Device List -->
            <TextBlock Text="Devices in Room" Style="{StaticResource HeadingM}" Margin="0,0,0,10"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="350">
                <ItemsControl ItemsSource="{Binding Devices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Height="70" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="50"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="160"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="White"/>
                                        <TextBlock Text="{Binding Status}" Foreground="{Binding StatusColor}" FontSize="12"/>
                                    </StackPanel>
                                    
                                    <!-- Temperature Control -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="10,0" Visibility="{Binding IsTemperatureControlVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="{Binding Temperature, StringFormat='{}{0}°C'}" HorizontalAlignment="Center" Style="{StaticResource SmallText}"/>
                                        <Slider Minimum="0" Maximum="30" Value="{Binding Temperature}" IsSnapToTickEnabled="True" TickFrequency="1"/>
                                    </StackPanel>
                                    
                                    <Button Grid.Column="3" Content="{Binding ActionButtonText}" Command="{Binding ToggleCommand}" Margin="5,0" Padding="10,4"/>
                                    <Button Grid.Column="4" Content="🗑" Command="{Binding DataContext.DeleteDeviceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="#EF4444" Margin="5,0" Padding="10,4"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>
