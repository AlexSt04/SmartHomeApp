<UserControl x:Class="SmartHouseApp.Views.Pages.DevicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <StackPanel>
            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Button Content="🔄 Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource {x:Type Button}}"/>
                <Button Content="✓ All ON" Command="{Binding ToggleAllCommand}" Style="{StaticResource SecondaryButton}" Margin="15,0"/>
                <Button Content="✕ All OFF" Command="{Binding ToggleAllCommand}" Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
            
            <!-- Device List -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="500">
                <ItemsControl ItemsSource="{Binding AllDevices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Height="80" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Icon -->
                                    <TextBlock Grid.Column="0" 
                                               Text="{Binding Icon}" 
                                               FontSize="28"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"/>
                                    
                                    <!-- Info -->
                                    <StackPanel Grid.Column="1" Margin="15,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource HeadingM}"/>
                                        <TextBlock Text="{Binding Room}" Style="{StaticResource SmallText}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    
                                    <!-- Status -->
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding Status}"
                                               Foreground="{Binding StatusColor}"
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"
                                               Margin="0,0,25,0"/>
                                    
                                    <!-- Action -->
                                    <Button Grid.Column="3" 
                                            Content="{Binding ActionButtonText}"
                                            Command="{Binding ToggleCommand}"
                                            Padding="20,8"
                                            VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>
