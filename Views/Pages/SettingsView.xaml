<UserControl x:Class="SmartHouseApp.Views.Pages.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <StackPanel MaxWidth="600" HorizontalAlignment="Left">
            <TextBlock Text="General Settings" Style="{StaticResource HeadingL}" Margin="0,0,0,25"/>
            
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="System Configuration" Style="{StaticResource HeadingM}" Margin="0,0,0,15"/>
                    
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="House Name" VerticalAlignment="Center" Foreground="White"/>
                        <TextBox Grid.Column="1" Text="{Binding SystemName}" Tag="Enter house name..."/>
                    </Grid>
                    
                    <Grid Margin="0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Notifications" VerticalAlignment="Center" Foreground="White"/>
                        <CheckBox Grid.Column="1" IsChecked="{Binding NotificationsEnabled}" Content="Enable push notifications" Foreground="White" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <TextBlock Text="Maintenance" Style="{StaticResource HeadingL}" Margin="0,10,0,15"/>
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,30">
                <StackPanel>
                    <TextBlock Text="Database &amp; Logs" Style="{StaticResource HeadingM}" Margin="0,0,0,15"/>
                    <Button Content="Clear System Logs" Command="{Binding ClearLogsCommand}" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left" Padding="20,10"/>
                    <TextBlock Text="Current log size: 1.2 MB" Style="{StaticResource SmallText}" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            
            <Button Content="💾 Save All Changes" Command="{Binding SaveCommand}" Width="200" HorizontalAlignment="Left" Padding="0,15"/>
        </StackPanel>
    </Grid>
</UserControl>
