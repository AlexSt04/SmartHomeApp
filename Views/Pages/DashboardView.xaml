<UserControl x:Class="SmartHouseApp.Views.Pages.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="Smart Home Overview" Style="{StaticResource SmallText}" Margin="0,0,0,20"/>
            
            <!-- Stats Row -->
            <UniformGrid Columns="3" Margin="0,0,0,30">
                <Border Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="Rooms" Style="{StaticResource SmallText}"/>
                        <TextBlock Text="{Binding RoomCount}" Style="{StaticResource HeadingXL}" Foreground="{StaticResource AccentSecondaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Margin="5,0">
                    <StackPanel>
                        <TextBlock Text="Devices" Style="{StaticResource SmallText}"/>
                        <TextBlock Text="{Binding DeviceCount}" Style="{StaticResource HeadingXL}" Foreground="{StaticResource AccentPrimaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Active Lights" Style="{StaticResource SmallText}"/>
                        <TextBlock Text="{Binding ActiveLightsCount}" Style="{StaticResource HeadingXL}" Foreground="{StaticResource StatusWarningBrush}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
            
            <!-- Room Cards Grid -->
            <TextBlock Text="Your Rooms" Style="{StaticResource HeadingL}" Margin="0,0,0,15"/>
            <ItemsControl ItemsSource="{Binding Rooms}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Width="180" MinHeight="160" Margin="0,0,15,15">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="🏠" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding Name}" Style="{StaticResource HeadingM}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding DeviceCount, StringFormat='{}{0} devices'}" Style="{StaticResource SmallText}" HorizontalAlignment="Center"/>
                                <Button Content="View ➜" Command="{Binding ViewDetailsCommand}" Style="{StaticResource SecondaryButton}" Margin="0,10,0,0" Padding="15,6" FontSize="12"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
            <!-- Recent Activity -->
            <TextBlock Text="Recent Activity" Style="{StaticResource HeadingL}" Margin="0,20,0,15"/>
            <Border Style="{StaticResource CardStyle}">
                <ItemsControl ItemsSource="{Binding RecentActivity}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,10">
                                <TextBlock Text="{Binding}" Style="{StaticResource BodyText}" Foreground="{StaticResource TextSecondaryBrush}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
