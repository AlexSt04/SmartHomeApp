<UserControl x:Class="SmartHouseApp.Views.Pages.RoomsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <StackPanel>
            <!-- Toolbar -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="+ Add Room" Command="{Binding AddRoomCommand}" Style="{StaticResource {x:Type Button}}"/>
                
                <TextBox Grid.Column="1" Margin="15,0" Tag="Search rooms..." x:Name="SearchBox"/>
                
                <ComboBox Grid.Column="2" Width="150" Height="38" VerticalContentAlignment="Center">
                    <ComboBoxItem Content="Sort by Name" IsSelected="True"/>
                    <ComboBoxItem Content="Sort by Devices"/>
                </ComboBox>
            </Grid>
            
            <!-- Table Header -->
            <Border Background="{StaticResource CardBackgroundBrush}" Padding="15,10" CornerRadius="8,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Room Name" Style="{StaticResource HeadingM}"/>
                    <TextBlock Grid.Column="1" Text="Devices" Style="{StaticResource HeadingM}" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="Status" Style="{StaticResource HeadingM}" HorizontalAlignment="Center"/>
                    <TextBlock Grid.Column="3" Text="Actions" Style="{StaticResource HeadingM}" HorizontalAlignment="Right"/>
                </Grid>
            </Border>
            
            <!-- Room List -->
            <ItemsControl ItemsSource="{Binding Rooms}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SecondaryBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="15" Margin="0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0" Text="{Binding Name}" Style="{StaticResource BodyText}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding DeviceCount}" Style="{StaticResource BodyText}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                
                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Ellipse Width="10" Height="10" Fill="{StaticResource StatusActiveBrush}" Margin="0,0,8,0"/>
                                    <TextBlock Text="Active" Style="{StaticResource BodyText}"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="✎" Margin="0,0,15,0" FontSize="18" Cursor="Hand" Foreground="{StaticResource AccentSecondaryBrush}"/>
                                    <TextBlock Text="✕" FontSize="18" Cursor="Hand" Foreground="{StaticResource StatusInactiveBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
